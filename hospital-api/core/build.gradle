group 'com.romanov'
version '0.1'

buildscript {
    repositories {
        mavenCentral()
    }
}

apply plugin: 'java'
apply plugin: 'org.springframework.boot'

bootJar.enabled = false

jar {
    baseName = 'core'
    enabled = true
    version = '0.1'
}

configurations {
    tests
}